<div class="modal fade" id="flatModal<%= flat.id unless flat.new_record? %>" tabindex="-1" aria-labelledby="flatModalLabel" aria-hidden="true" data-controller="modal" data-modal-type-value="<%= flat.new_record? ? 'add' : 'edit' %>">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="flatModalLabel"><%= flat.new_record? ? "Créer un logement" : "Modifier le logement" %></h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
      </div>
      <div class="modal-body">
        <%= render partial: "flats/form", locals: { flat: flat } %>
      </div>
    </div>
  </div>
</div>
